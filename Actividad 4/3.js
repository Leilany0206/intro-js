// Crea el mismo problema 1, ahora usando Do While

var limite = prompt("Ingresa un número");
var num = 0;
var div = (limite / 5)

do {
    num = num + 5; 
    console.log (num)
}
while (num < (5 * div.toFixed())) 